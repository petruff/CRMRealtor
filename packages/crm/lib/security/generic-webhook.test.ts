import{describe,expect,it}from'vitest';import{createHmac}from'node:crypto';import{verifyGenericWebhook,webhookSecretVerifier}from'./generic-webhook.ts';
describe('generic webhook verification',()=>{it('verifies current or overlap secret and exact body signature',()=>{const body=new TextEncoder().encode('{"ok":true}');const secret='s'.repeat(32);const pepper='p'.repeat(32);const signature=`sha256=${createHmac('sha256',secret).update(body).digest('hex')}`;expect(verifyGenericWebhook({mode:'hmac-sha256',secret,storedVerifiers:[webhookSecretVerifier(secret,pepper)],pepper,body,signature})).toBe(true);expect(verifyGenericWebhook({mode:'hmac-sha256',secret,storedVerifiers:[webhookSecretVerifier(secret,pepper)],pepper,body:new TextEncoder().encode('{}'),signature})).toBe(false);});});
