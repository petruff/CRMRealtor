import { describe,expect,it } from 'vitest';import { behaviorRecommendation,cmaInitialState,validateCreateCmaRequest,validatePropertyBehavior } from './property-behavior';
const propertyId='49100000-0000-4000-8000-000000000091',contactId='49100000-0000-4000-8000-000000000092';
describe('property behavior and CMA governance',()=>{it('validates cited behavior without changing relationship state',()=>{expect(validatePropertyBehavior({propertyId,contactId,type:'showing-request',source:'website',sourceReference:'signed-form:A1B2C3D4',occurredAt:'2026-08-31T12:00:00Z',permissionState:'allowed',idempotencyKey:'behavior:event-1'}).type).toBe('showing-request');});it('proposes attention only from exact recent behavior',()=>{expect(behaviorRecommendation([{type:'view',occurredAt:'2026-08-30T12:00:00Z'},{type:'view',occurredAt:'2026-08-30T13:00:00Z'},{type:'view',occurredAt:'2026-08-30T14:00:00Z'}],new Date('2026-08-31T12:00:00Z'))).toMatchObject({eligible:true,eventCount:3});});it('keeps CMA blocked without active provider authority',()=>{expect(cmaInitialState({state:'pending'},'2026-08-31T12:00:00Z')).toEqual({state:'blocked',reason:'Licensed comparable access is not active.'});});it('bounds explicit CMA selection criteria',()=>{expect(validateCreateCmaRequest({propertyId,contactId,purpose:'seller-consultation',criteria:{radiusMiles:2,lookbackDays:180,propertyKinds:['condo'],bedroomsTolerance:1,bathroomsTolerance:1,squareFeetTolerancePercent:20},asOf:'2026-08-31T12:00:00Z',idempotencyKey:'cma:request-0001'}).criteria.lookbackDays).toBe(180);});});
