import{describe,expect,it}from'vitest';import{createGovernedContactExport,spreadsheetSafe}from'./governed-export.ts';import type{Contact}from'../domain/contact.ts';
const contact={id:'c1',firstName:'=CMD()',lastName:'Test',leadType:'warm',relationship:'lead',intent:'unknown',source:'other',pipelineStage:'new',touchDateOverridden:false,tags:[],emailSubscribed:false,createdAt:'2026-01-01',updatedAt:'2026-01-01'} satisfies Contact;
describe('governed export',()=>{it('neutralizes spreadsheet formula injection',()=>{expect(spreadsheetSafe('=1+1')).toBe("'=1+1");const result=createGovernedContactExport([contact],{format:'csv',fields:['firstName','email']});expect(new TextDecoder().decode(result.bytes)).toContain("'=CMD()");});it('rejects internal fields',()=>expect(()=>createGovernedContactExport([contact],{format:'csv',fields:['ownerId']})).toThrow('allowlist'));});
